{
  "baseBranch": "main",
  "maxParallel": 2,
  "maxIssuesPerRun": 5,
  "issueTimeoutMin": 30,
  "model": "opus",
  "setup": "rm -f package-lock.json && npm install --no-audit --no-fund && git checkout -- package-lock.json 2>/dev/null; true",
  "labels": { "ready": "ready-for-agent", "human": "ready-for-human" }
}
