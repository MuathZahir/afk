{
  "name": "afk",
  "version": "0.2.0",
  "type": "module",
  "description": "Drain a GitHub ready-for-agent issue queue with isolated Claude Code workers. One milestone → one feature branch → one PR. Runs on your Claude subscription.",
  "bin": {
    "afk": "bin/afk.mjs"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@ai-hero/sandcastle": "^0.7.0",
    "tsx": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "typescript": "^6.0.3"
  }
}
